------------------------------------
create schema testseries;
------------------------------------
CREATE TABLE testseries.categorydetails(
    categoryid int NOT NULL AUTO_INCREMENT,
    categoryname varchar(25),
    PRIMARY KEY (categoryid)
);
------------------------------------
insert into testseries.categorydetails (categoryname) values('C');
insert into testseries.categorydetails (categoryname) values('CPP');
insert into testseries.categorydetails (categoryname) values('OS');
insert into testseries.categorydetails (categoryname) values('JAVA');
------------------------------------
CREATE TABLE testseries.users(
    userid int NOT NULL AUTO_INCREMENT,
    firstname varchar(50),
	lastname varchar(50),
	emailid varchar(50),
	username varchar(50),
	password varchar(50),
    PRIMARY KEY (userid)
);

